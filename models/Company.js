class Company {
    constructor(
        id,
        name,
        banner
    ){
        this.id = id,
        this.name = name,
        this.banner = banner
    }
}

export default Company