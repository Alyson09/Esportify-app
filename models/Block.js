class Block {
    constructor(
        id,
        title,
        banner,
        endereco
    ){
        this.id = id,
        this.title = title,
        this.banner = banner,
        this.endereco= endereco
    }
}

export default Block