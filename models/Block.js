class Block {
    constructor(
        id,
        title,
        banner,
        rua,
        numero,
        cep
    ){
        this.id = id,
        this.title = title,
        this.banner = banner,
        this.rua = rua,
        this.numero = numero,
        this.cep = cep
    }
}

export default Block